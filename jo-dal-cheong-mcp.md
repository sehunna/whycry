# Draft: 조달청 입찰정보서비스 API 기반 MCP

## Requirements (confirmed)
- 사용자는 AI 교육 정보화 사업을 하는 사업자다.
- 조달청의 입찰정보서비스 API를 이용해 MCP를 만들고 싶다.
- 사용자가 원하는 정보/가치를 얻을 수 있는 좋은 서비스 구성을 알고 싶다.
- 가장 중요한 목표는 1) 새 입찰 찾아내기, 2) 내가 참여할 만한 것만 선별하기다.
- 선별 기준은 지자체, 서울/경기, 1억 이상, 용역, 모든계약, 소프트웨어 분야다.
- 알림/요약은 매일 1회 받는 방식이 좋다.
- 결과 확인 채널은 콘솔/대시보드만 사용한다.
- 대시보드 형태는 웹형이 좋다.
- 웹 대시보드 필수 항목은 신규공고목록, 적합도 점수, 제외/보류/추천 이유다.
- 대시보드에 예산도 함께 보여준다.
- 예산은 공고 원문 금액 그대로 보여준다.
- 주요 사용자는 영업 담당이다.

## Technical Decisions
- 아직 결정되지 않음: 어떤 조달 정보(입찰공고, 개찰결과, 낙찰, 사전규격 등)를 우선 대상으로 할지.
- 아직 결정되지 않음: MCP가 어떤 인터페이스를 제공할지(질의응답형, 알림형, 대시보드형, 자동요약형).
- 우선 제공 기능: 신규 공고 탐색, 적합도 판정, 웹 대시보드 일일 브리핑.
- 모듈 분리 방향: 수집, 정규화, 필터링, 적합도 평가, 랭킹, 대시보드, 스케줄러로 나눈다.
- 적합도 평가는 규칙 기반을 기본으로 하고, 나중에 LLM 요약/설명을 붙인다.
- 대시보드는 영업 담당 기준으로 "오늘 볼 것" 중심의 단순한 화면으로 간다.
- 실제 구현 기본값: OpenCode MCP, Node.js/TypeScript, SQLite, 환경변수 기반 API 키.
- API 키는 저장소에 커밋하지 않고 `.env` 또는 OpenCode MCP environment로만 주입한다.

## Proposed Modules
- **API Connector**: 조달청 입찰정보서비스 API 호출, 인증/에러 처리, 원천 데이터 수집
- **Normalizer**: API 응답을 공통 필드(제목, 기관, 금액, 지역, 계약유형, 마감일 등)로 변환
- **Rule Filter**: 지자체/서울경기/1억 이상/용역/모든계약/소프트웨어 조건 적용
- **Scoring Engine**: 적합도 점수 계산 및 추천/보류/제외 판정
- **Reason Generator**: 판정 이유를 한 줄 또는 짧은 문단으로 생성
- **Daily Digest**: 하루 1회 요약 리스트 생성
- **Web Dashboard**: 신규공고목록, 적합도 점수, 제외/보류/추천 이유, 예산 표시
- **Scheduler**: 매일 지정 시각에 수집 및 요약 실행
- **Storage**: 이전 공고, 판정 결과, 브리핑 이력 저장

## Research Findings
- 조달청 '나라장터 입찰공고정보서비스'(15129394)는 입찰공고 목록/상세, 기초금액, 면허제한, 참가가능지역, 변경이력을 제공한다.
- 업무구분별(물품/용역/공사/외자) 오퍼레이션을 맞춰 호출해야 한다.
- '공공데이터개방표준서비스'(15058815)는 입찰/낙찰/계약의 표준화된 분석·통합용 성격이 강하다.

## Open Questions
- 없음

## Scope Boundaries
- INCLUDE: 조달청 입찰정보서비스 API를 활용한 MCP 서비스 아이디어, 기능 우선순위, 추천 데이터 소스/서비스 구상
- EXCLUDE: 코드 구현, 실제 API 연동 개발, 입찰 법률 자문
